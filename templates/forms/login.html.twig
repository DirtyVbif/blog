{{ wrapper.addClass('form-wrapper form-wrapper_login').open() }}
    <form action="/user" method="POST" {{ id('form-login', true) }} class="form form_login">
        {{ csrf() }}
        <input type="hidden" name="type" value="login">
        <div class="form__field form_login__field">
            <label for="form-login--mail" class="form__field-label form_login__field-label">Логин:</label>
            <input
                type="email"
                {{ id('form-login--mail', true) }}
                name="mail"
                max="255"
                class="form__input form_login__input"
                required>
        </div>
        <div class="form__field form_login__field">
            <label for="form-login--password" class="form__field-label form_login__field-label">Пароль:</label>
            <input
                type="password"
                {{ id('form-login--password', true) }}
                name="password"
                max="40"
                class="form__input form_login__input"
                required>
        </div>
        <footer class="form__footer form_login__footer">
            <div class="form__field form_login__field form_login__field_submit">
                <input type="submit" value="Войти" class="btn btn_transparent form__submit form_login__submit">
            </div>

            <div class="form__field form__field_checkbox form_login__field form_login__field_checkbox">
                <input
                    type="checkbox"
                    {{ id('form-login--remember', true) }}
                    name="remember_me"
                    class="form__checkbox form_login__checkbox">
                <label for="form-login--remember" class="form__field-label form__field-label_checkbox form_login__field-label">Запомнить меня</label>
            </div>
        </footer>
    </form>
{{ wrapper.close() }}