{{ wrapper.addClass('form-wrapper form-wrapper_feedback').open() }}
    <form action="/" method="POST" {{ id('form-feedback', true) }} class="form form_feedback">
        {{ csrf() }}
        <input type="hidden" name="type" value="feedback">
        <div class="form__field form_feedback__field">
            <label for="form-feedback--name" class="form__field-label form_feedback__field-label">Ваше имя:</label>
            <input
                type="text"
                {{ id('form-feedback--name', true) }}
                name="name"
                max-length="60"
                class="form__input form_feedback__input"
                required{{ old('name', true) }}>
        </div>
        <div class="form__field form_feedback__field">
            <label for="form-feedback--mail" class="form__field-label form_feedback__field-label">Ваш e-mail:</label>
            <input
                type="email"
                {{ id('form-feedback--mail', true) }}
                name="email"
                max-length="256"
                class="form__input form_feedback__input"
                required{{ old('email', true) }}>
            <span class="form__field-anno form_feedback__field-anno">Он не будет отображаться где-либо на сайте. Это только для обратной связи с Вами.</span>
        </div>
        <div class="form__field form_feedback__field">
            <label for="form-feedback--subject" class="form__field-label form_feedback__field-label">Сообщение:</label>
            <textarea
                {{ id('form-feedback--subject', true) }}
                name="subject"
                class="form__textarea form_feedback__textarea"
                rows="6"
                required>{{ old('subject') }}</textarea>
        </div>
        <div class="form__field form_feedback__field">
            <input type="submit" value="Отправить" class="btn btn_transparent form__submit form_feedback__submit">
        </div>
    </form>

    <div class="form-wrapper__rules">
        <p>Пожалуйста воздержитесь от отправки сообщения, если оно содержит:</p>
        <ul>
            <li>Ругань и ненормативную лексику при отсутствии смысловой нагрузки;</li>
            <li>Рекламу (услуги, товары и пр.);</li>
            <li>Неинформативный текст, флуд, спам.</li>
        </ul>
        <p>Сообщения подобного характера будут проигнорированы.</p>
    </div>
{{ wrapper.close() }}